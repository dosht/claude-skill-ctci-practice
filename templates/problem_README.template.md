# {{problem_number}} — {{problem_title}}

**Chapter:** {{chapter_name}}  |  **Difficulty:** {{difficulty}}  |  **Topic:** `{{topic_slug}}`

---

## I/O contract (read this first)

**Function signature:** `{{function_signature}}`

**Input:** {{input_description}}

**Output:** {{output_description}}

**Constraints:** {{constraints}}

---

## Problem statement

{{problem_statement}}

## Examples

{{examples_block}}

## Edge cases to consider

{{edge_cases_list}}

## Complexity target

- **Time:** {{time_target}}
- **Space:** {{space_target}}

---

## How to work this problem

1. Read the I/O contract above carefully — this is what the tests check.
2. Open `solution.py` and implement `{{function_name}}` (starter has TODOs).
3. Run `pytest test_basic.py -v` while iterating. These are example cases.
4. When basic tests pass, run `pytest test_full.py -v`. These cover edge cases + perf.
5. Need a nudge? Say `/ctci-practice hint` — hints are context-aware against your current code.
6. Done? Say `/ctci-practice validate` — runs tests + reviews code, marks the problem complete on pass.
7. Totally stuck? `/ctci-practice solve` reveals the reference solution (counts as 0.5 toward topic score).
