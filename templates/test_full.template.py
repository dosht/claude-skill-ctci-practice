"""Full tests — edge cases, boundary conditions, and perf.

This file is the authoritative check (like a LeetCode "submit"). The
`/ctci-practice validate` verb runs these; all must pass to mark the
problem complete.

Run: pytest test_full.py -v
"""
import pytest

from solution import {{function_name}}


{{full_test_cases}}
